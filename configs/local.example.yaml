# Copy this file to configs/local.yaml and fill in local-only secrets.
# configs/local.yaml is intentionally ignored by git.

model:
  api_key: "replace-with-your-dashscope-key"
  default_base_url: "https://dashscope.aliyuncs.com/compatible-mode/v1"
  default_agent_model: "qwen3.6-plus"
  default_judge_model: "qwen-plus"

